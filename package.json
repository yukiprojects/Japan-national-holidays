{
  "name": "holiday-fetcher",
  "version": "1.0.0",
  "description": "Fetch Japanese holidays from Google Calendar API",
  "main": "fetch-holidays.js",
  "type": "module",  // これを追加
  "scripts": {
    "start": "node fetch-holidays.js"
  },
  "dependencies": {
    "node-fetch": "^3.0.0"
  }
}
