{
  "name": "holiday-fetcher",
  "version": "1.0.0",
  "description": "Fetch Japanese holidays from Google Calendar API",
  "main": "fetch-holidays.mjs",  // 拡張子を .mjs に修正
  "type": "module", 
  "scripts": {
    "start": "node fetch-holidays.mjs"  // こちらも .mjs に修正
  },
  "dependencies": {
    "node-fetch": "^3.0.0"
  }
}
